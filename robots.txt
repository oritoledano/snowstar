# snowstar.company
User-agent: *
Allow: /

# The API answers JSON and the dashboard and account pages are one person's own
# records. /t/ USED to be a share-card redirect and was blocked here for that
# reason — it is now the only indexable page each of the 400 tracks has, and
# the sitemap lists every one of them, so blocking it here would have quietly
# forbidden exactly the work the sitemap was pointing at.
Disallow: /api/
Disallow: /dashboard.html
Disallow: /account.html

Sitemap: https://snowstar.company/sitemap.xml
